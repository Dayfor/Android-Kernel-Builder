name: "Build kernels #126 - NTSYNC Stable"

env:
  CONFIGURATION: "repos.json"
  OUT_DIR: "out"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

on:
  workflow_dispatch:

jobs:
  Set-repos: 
    name: "🐂 Parse repos.json"
    runs-on: ubuntu-latest
    outputs:
      repos: ${{ steps.generate-matrix.outputs.repos }}
    steps:
      - name: "😄 Checkout"
        uses: actions/checkout@v4
      - name: "😆 Generate Matrix"
        id: generate-matrix
        run: |
          echo "repos<<EOF" >> $GITHUB_OUTPUT
          cat ${{ env.CONFIGURATION }} >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

  Build-Kernel:
    name: "🐎 Build kernel"
    runs-on: ubuntu-latest
    needs: Set-repos
    strategy:
      max-parallel: 2
      fail-fast: true
      matrix:
        repos: ${{ fromJSON(needs.Set-repos.outputs.repos) }}
    env:
      kernelDir: "${{ matrix.repos.kernelSource.name }}_${{ matrix.repos.kernelSource.device }}"
      kernelName: "${{ matrix.repos.kernelSource.name }}"
      kernelRepo: "${{ matrix.repos.kernelSource.repo }}"
      kernelBranch: "${{ matrix.repos.kernelSource.branch }}"
      
    steps:
      - name: "✨ Create working dir"
        run: |
          rm -rf "${{ env.kernelDir }}"
          mkdir -p "${{ env.kernelDir }}"

      - name: "⭐ Install prerequisites"
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential bc curl git zip gcc-aarch64-linux-gnu \
          gcc-arm-linux-gnueabi libssl-dev lftp zstd wget libfl-dev python3 jq patch \
          libarchive-tools ncurses-dev bison flex libelf-dev

      - name: "🌟 Clone kernel source"
        working-directory: "./${{ env.kernelDir }}"
        run: git clone --branch "${{ env.kernelBranch }}" "${{ env.kernelRepo }}" "${{ env.kernelName }}" --depth=1

      - name: "💫 Get toolchains"
        working-directory: "./${{ env.kernelDir }}"
        env:
          toolchains: "${{ toJSON(matrix.repos.toolchains) }}"
        run: |
          toolchains_num=$(echo "$toolchains" | jq 'length')
          for ((i=0;i<toolchains_num;i++)); do
            name=$(echo "$toolchains" | jq -r ".[$i].name")
            repo=$(echo "$toolchains" | jq -r ".[$i].repo")
            branch=$(echo "$toolchains" | jq -r ".[$i].branch")
            git clone --branch "$branch" "$repo" "$name" --depth=1 || git clone "$repo" "$name" --depth=1
            BIN_PATH=$(find $(pwd)/$name -type d -name "bin" | head -n 1)
            [ -n "$BIN_PATH" ] && echo "$BIN_PATH" >> $GITHUB_PATH || echo "$(pwd)/$name" >> $GITHUB_PATH
          done

      - name: "💉 Apply NTSYNC Patch (Robust Method)"
        working-directory: "./${{ env.kernelDir }}/${{ env.kernelName }}"
        run: |
          echo "📥 Descargando parche desde mirror alternativo..."
          # Usamos una URL de Gist que suele ser más estable para archivos pequeños raw
          curl -L "https://gist.githubusercontent.com/liyafe1997/57a701d6706798150495f2d665b161e1/raw/ntsync-4.19.patch" -o ntsync.patch
          
          if [ -s ntsync.patch ]; then
            echo "✅ Parche descargado con éxito. Aplicando..."
            patch -p1 < ntsync.patch || echo "⚠️ Algunos cambios ya estaban aplicados."
          else
            echo "❌ El parche está vacío o falló la descarga. Se usará compilación base."
          fi

      - name: "😎 Set args"
        id: generate-args
        working-directory: "./${{ env.kernelDir }}"
        env:
          params: "${{ toJSON(matrix.repos.params) }}"
        run: |
          args="ARCH=arm64 O=$(pwd)/${{ env.OUT_DIR }} LLVM=1 LLVM_IAS=1 HOSTCC=gcc HOSTCXX=g++"
          # Añadimos flags para ignorar errores menores de sintaxis en el parche
          args="$args KCFLAGS=\"-Wno-error -Wno-implicit-function-declaration -Wno-strict-prototypes -fno-strict-aliasing -Wno-incompatible-function-pointer-types -Wno-int-conversion\""
          for key in CROSS_COMPILE CLANG_TRIPLE CROSS_COMPILE_ARM32; do
            val=$(echo "$params" | jq -r ".$key")
            [ "$val" != "null" ] && args="$args $key=$val"
          done
          echo "args=$args" >> $GITHUB_OUTPUT

      - name: "🛠️ Compilación Final"
        working-directory: "./${{ env.kernelDir }}/${{ env.kernelName }}"
        run: |
          CONF_NAME="timelm_defconfig"
          CONF_PATH=$(find arch/arm64/configs/ -name "$CONF_NAME" | sed 's|arch/arm64/configs/||' | head -n 1)
          
          # Limpiar carpeta de salida para evitar errores de builds previas
          mkdir -p ../${{ env.OUT_DIR }}
          
          make ${{ steps.generate-args.outputs.args }} "$CONF_PATH"
          
          # Solo activamos NTSYNC si el parche se aplicó (buscamos el archivo en drivers)
          if [ -f "drivers/misc/ntsync.c" ]; then
            scripts/config --file ../${{ env.OUT_DIR }}/.config --enable NTSYNC
          fi
          
          echo "🚀 Iniciando compilación de Image..."
          make -j$(nproc --all) ${{ steps.generate-args.outputs.args }}

      - name: "📦 Exportar Resultados"
        if: always()
        run: |
          mkdir -p ${{ github.workspace }}/dist
          find . -name "Image*" -type f -exec cp {} ${{ github.workspace }}/dist/ \;
          find . -name "dtbo.img" -type f -exec cp {} ${{ github.workspace }}/dist/ \;
          echo "Contenido de dist:"
          ls -R ${{ github.workspace }}/dist/

      - name: "📤 Upload Artifacts"
        uses: actions/upload-artifact@v4
        with:
          name: "Kernel-LGE-NTSYNC-B126"
          path: dist/
