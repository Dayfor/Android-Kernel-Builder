name: "Build kernels"

env:
  CONFIGURATION: "repos.json"
  OUT_DIR: "out"

on:
  workflow_dispatch:

jobs:
  Set-repos:
    name: "🐂 Parse repos.json"
    runs-on: ubuntu-latest
    outputs:
      repos: ${{ steps.generate-matrix.outputs.repos }}
    steps:
      - name: "😄 Checkout"
        uses: actions/checkout@v3

      - name: "😆 Generate Matrix"
        id: generate-matrix
        run: |
          echo "repos<<EOF" >> $GITHUB_OUTPUT
          cat  ${{ env.CONFIGURATION }} >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

  Build-Kernel:
    name: "🐎 Build kernel"
    runs-on: ubuntu-latest
    needs: Set-repos
    strategy:
      matrix:
        repos: ${{ fromJSON(needs.Set-repos.outputs.repos) }}
    env:
      kernelDir: ${{ matrix.repos.kernelSource.name }}_${{ matrix.repos.kernelSource.device }}
      kernelName: ${{ matrix.repos.kernelSource.name }}
      kernelRepo: ${{ matrix.repos.kernelSource.repo }}
      kernelBranch: ${{ matrix.repos.kernelSource.branch }}
      kernelDevice: ${{ matrix.repos.kernelSource.device }}
    steps:
      - name: "✨ Create working dir"
        run: mkdir -p $kernelDir

      - name: "⭐ Install prerequisites"
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential bc curl git zip gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi libssl-dev lftp zstd wget libfl-dev python3 libarchive-tools libdebuginfod-dev jq

      - name: "🌟 Clone kernel source"
        working-directory: ./${{ env.kernelDir }}
        run: |
          git clone --recursive --branch ${{ env.kernelBranch }} ${{ env.kernelRepo }} ${{ env.kernelName }} --depth=1 || \
          git clone --recursive ${{ env.kernelRepo }} ${{ env.kernelName }} --depth=1

      - name: "💉 Aplicar Parche NTSYNC y Android 15"
        working-directory: ./${{ env.kernelDir }}/${{ env.kernelName }}
        run: |
          # Descargar parche de sincronización
          wget https://raw.githubusercontent.com/Project-Vortex/android_kernel_patches/main/ntsync/ntsync.patch
          patch -p1 --ignore-whitespace < ntsync.patch || echo "Conflicto menor en parche, ignorando..."
          
          # Buscar defconfig (Lineage usa carpetas vendor/)
          REAL_CONFIG=$(find arch/arm64/configs/ -name "*lito*" | grep "perf" | head -n 1)
          [ -z "$REAL_CONFIG" ] && REAL_CONFIG=$(find arch/arm64/configs/ -name "v60_defconfig" | head -n 1)
          
          if [ -n "$REAL_CONFIG" ]; then
            echo "Configurando $REAL_CONFIG para Android 15..."
            echo -e "\nCONFIG_NTSYNC=y\nCONFIG_BPF_SYSCALL=y\nCONFIG_USERFAULTFD=y\nCONFIG_MEMBARRIER=y\nCONFIG_ANDROID_BINDER_IPC=y" >> "$REAL_CONFIG"
          fi

      - name: "💫 Get toolchains"
        working-directory: ./${{ env.kernelDir }}
        env:
          toolchains: "${{ toJSON(matrix.repos.toolchains) }}"
        run: |
          toolchains_num="$(echo $toolchains | jq 'length')"
          for ((i=0;i<toolchains_num;i++)); do
            name=$(echo $toolchains | jq -r ".[$i].name")
            repo=$(echo $toolchains | jq -r ".[$i].repo")
            branch=$(echo $toolchains | jq -r ".[$i].branch")
            git clone --branch $branch $repo $name --depth=1
            echo "$(pwd)/$name/bin" >> $GITHUB_PATH
          done

      - name: "😎 Set args"
        id: generate-args
        working-directory: ./${{ env.kernelDir }}
        env:
          params: "${{ toJSON(matrix.repos.params) }}"
        run: |
          mkdir -p $(pwd)/${{ env.OUT_DIR }}
          BUILD_ARGS="ARCH=arm64 CROSS_COMPILE=$(echo $params | jq -r ".CROSS_COMPILE") CROSS_COMPILE_ARM32=$(echo $params | jq -r ".CROSS_COMPILE_ARM32") O=$(pwd)/${{ env.OUT_DIR }}"
          BUILD_ARGS="$BUILD_ARGS CC=clang CLANG_TRIPLE=$(echo $params | jq -r ".CLANG_TRIPLE")"
          BUILD_ARGS="$BUILD_ARGS NM=llvm-nm OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip"
          echo "args=-j2 $BUILD_ARGS" >> $GITHUB_OUTPUT

      - name: "👍 Make defconfig"
        working-directory: ./${{ env.kernelDir }}/${{ env.kernelName }}
        run: |
          CONF_NAME=$(find arch/arm64/configs/ -name "*lito*" | grep "perf" | printf "%P\n" | head -n 1)
          [ -z "$CONF_NAME" ] && CONF_NAME="vendor/lito-perf_defconfig"
          make ${{ steps.generate-args.outputs.args }} $CONF_NAME

      - name: "🎶 Build kernel"
        working-directory: ./${{ env.kernelDir }}/${{ env.kernelName }}
        run: make ${{ steps.generate-args.outputs.args }}

      - name: "💛 Upload"
        uses: actions/upload-artifact@v4
        with:
          name: Kernel-Lineage-V60-NTSYNC
          path: ./${{ env.kernelDir }}/${{ env.OUT_DIR }}/arch/arm64/boot/Image*
          
